import { DefaultImagePipePipe } from './default-image-pipe.pipe';

describe('DefaultImagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultImagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
